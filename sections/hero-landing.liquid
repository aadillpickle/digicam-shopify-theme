{%- style -%}
  .hero-landing-section {
    position: relative;
    min-height: 100vh;
    display: flex;
    flex-direction: column;
    justify-content: flex-end;
    padding: 3rem;
    overflow: hidden;
    background: {{ section.settings.background_color }};
  }

  .hero-landing-section::before {
    content: '';
    position: absolute;
    bottom: 0;
    left: 0;
    width: 70%;
    height: 60%;
    background: radial-gradient(
      ellipse at bottom left,
      {{ section.settings.gradient_peach }} 0%,
      transparent 70%
    );
    opacity: 0.7;
    pointer-events: none;
  }

  .hero-landing-section::after {
    content: '';
    position: absolute;
    top: 0;
    right: 0;
    width: 60%;
    height: 100%;
    background: radial-gradient(
      ellipse at top right,
      {{ section.settings.gradient_lavender }} 0%,
      transparent 70%
    );
    opacity: 0.7;
    pointer-events: none;
  }

  .hero-cta-wrapper {
    position: absolute;
    left: {{ section.settings.button_horizontal_position }}%;
    top: {{ section.settings.button_vertical_position }}%;
    transform: translateX(-50%);
    z-index: 10;
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-cta-arrow {
    width: {{ section.settings.arrow_width }}px;
    height: {{ section.settings.arrow_length }}px;
    stroke: var(--color-text, #1a1a1a);
    stroke-width: 2;
    fill: none;
    margin-top: {{ section.settings.arrow_offset }}px;
    transform: rotate({{ section.settings.arrow_rotation }}deg);
  }

  .hero-cta-wrapper .btn {
    padding: 1.25rem 2.5rem;
    font-size: 1.0625rem;
  }

  #hero-3d-canvas {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    pointer-events: none;
    z-index: 1;
  }

  .circular-text-container .circular-text text {
    font-size: {{ section.settings.circular_text_size }}px;
  }

  @media (max-width: 768px) {
    .hero-landing-section {
      padding: 1.5rem;
      padding-top: 100px;
      min-height: 100svh;
      display: flex;
      flex-direction: column;
      justify-content: flex-start;
      align-items: center;
      --mobile-headline-size: {{ section.settings.mobile_headline_size }}rem;
    }

    .hero-content {
      order: 1;
      text-align: center;
      padding-bottom: 0;
      margin-top: var(--spacing-lg);
    }

    .hero-headline {
      text-align: center;
      font-size: var(--mobile-headline-size, 3rem);
    }

    .hero-product {
      order: 2;
      position: relative;
      top: auto;
      right: auto;
      transform: rotate(12deg);
      width: 70%;
      max-width: 280px;
      margin: var(--spacing-lg) auto;
    }

    .hero-cta-wrapper {
      order: 3;
      position: relative;
      left: auto;
      top: auto;
      transform: none;
      margin: var(--spacing-lg) auto;
      width: 100%;
      display: flex;
      justify-content: center;
    }

    .hero-cta-wrapper .btn {
      width: auto;
      max-width: 280px;
    }

    .hero-cta-arrow {
      {%- if section.settings.arrow_mobile_visible == false -%}
        display: none;
      {%- else -%}
        display: block;
      {%- endif -%}
      width: {{ section.settings.arrow_mobile_width | default: 30 }}px;
      height: {{ section.settings.arrow_mobile_length | default: 40 }}px;
      margin-top: 8px;
      transform: rotate({{ section.settings.arrow_mobile_rotation | default: 0 }}deg);
    }

    .circular-text-container {
      order: 4;
      position: absolute;
      top: 85px;
      right: 1rem;
      left: auto;
      width: 50px;
      height: 50px;
      margin: 0;
    }

    .circular-text-container .circular-lens {
      width: 25px;
      height: 25px;
    }

    .circular-text-container .circular-text text {
      font-size: {{ section.settings.circular_text_mobile_size }}px;
    }

    #hero-3d-canvas {
      opacity: 1;
      z-index: 1;
    }
  }
{%- endstyle -%}

<section class="hero-landing-section" data-section-id="{{ section.id }}">
  <canvas id="hero-3d-canvas"></canvas>

  <!-- Circular Text Element with Lens -->
  <div class="circular-text-container">
    <svg class="circular-text" viewBox="0 0 150 150">
      <defs>
        <path id="circlePath" d="M 75, 75 m -60, 0 a 60,60 0 1,1 120,0 a 60,60 0 1,1 -120,0"/>
      </defs>
      <text>
        <textPath href="#circlePath">
          {{ section.settings.circular_text | default: 'point and shoot  point and shoot  ' }}
        </textPath>
      </text>
    </svg>
    <div class="circular-lens">
      {%- if section.settings.lens_image != blank -%}
        <img
          src="{{ section.settings.lens_image | image_url: width: 120 }}"
          alt="{{ section.settings.lens_image.alt | default: 'Camera lens' }}"
          width="60"
          height="60"
          loading="eager"
        >
      {%- else -%}
        <img
          src="data:image/svg+xml,%3Csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'%3E%3Ccircle cx='50' cy='50' r='45' fill='%231a1a1a'/%3E%3Ccircle cx='50' cy='50' r='35' fill='%23333'/%3E%3Ccircle cx='50' cy='50' r='20' fill='%23555'/%3E%3Ccircle cx='50' cy='50' r='10' fill='%23222'/%3E%3Ccircle cx='40' cy='40' r='5' fill='%23667799' opacity='0.5'/%3E%3C/svg%3E"
          alt="Camera lens"
          width="60"
          height="60"
          loading="eager"
        >
      {%- endif -%}
    </div>
  </div>

  <!-- Product Image -->
  <div class="hero-product">
    {%- if section.settings.product_image != blank -%}
      <img
        src="{{ section.settings.product_image | image_url: width: 1200 }}"
        alt="{{ section.settings.product_image.alt | default: shop.name }}"
        width="600"
        height="600"
        loading="eager"
      >
    {%- else -%}
      <img
        src="{{ 'hero-camera.webp' | asset_url }}"
        alt="{{ shop.name }}"
        width="600"
        height="600"
        loading="eager"
      >
    {%- endif -%}
  </div>

  <div class="hero-cta-wrapper">
    {%- liquid
      if section.settings.button_link != blank
        assign button_url = section.settings.button_link
      elsif section.settings.featured_product != blank
        assign button_url = section.settings.featured_product.url
      else
        assign button_url = routes.root_url
      endif
    -%}
    <a href="{{ button_url }}" class="btn btn-primary btn-with-check btn-large">
      {{ section.settings.button_text | default: 'Add to cart' }}
    </a>

    <svg class="hero-cta-arrow" viewBox="0 0 50 {{ section.settings.arrow_length }}" xmlns="http://www.w3.org/2000/svg" preserveAspectRatio="xMidYMid meet">
      <path d="M 25 {{ section.settings.arrow_length | minus: 5 }} Q 20 {{ section.settings.arrow_length | divided_by: 2 }} 25 15" stroke-linecap="round" fill="none"/>
      <path d="M 18 22 L 25 12 L 32 22" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
    </svg>
  </div>

  <!-- Hero Headline -->
  <div class="hero-content">
    <h1 class="hero-headline">
      <span class="hero-headline-italic">{{ section.settings.headline_line1 | default: 'Mini' }}</span>
      <span>{{ section.settings.headline_line2 | default: 'DigiCam' }}</span>
    </h1>
  </div>
</section>

<script id="hero-3d-settings" type="application/json">
  {
    "sphere1": { 
      "x": {{ section.settings.sphere1_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.sphere1_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.sphere1_mobile_x | default: section.settings.sphere1_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.sphere1_mobile_y | default: section.settings.sphere1_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.sphere1_rotation }}, 
      "visible": {{ section.settings.sphere1_visible }},
      "mobileVisible": {{ section.settings.sphere1_mobile_visible | default: section.settings.sphere1_visible }}
    },
    "sphere2": { 
      "x": {{ section.settings.sphere2_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.sphere2_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.sphere2_mobile_x | default: section.settings.sphere2_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.sphere2_mobile_y | default: section.settings.sphere2_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.sphere2_rotation }}, 
      "visible": {{ section.settings.sphere2_visible }},
      "mobileVisible": {{ section.settings.sphere2_mobile_visible | default: section.settings.sphere2_visible }}
    },
    "torus": { 
      "x": {{ section.settings.torus_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.torus_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.torus_mobile_x | default: section.settings.torus_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.torus_mobile_y | default: section.settings.torus_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.torus_rotation }}, 
      "visible": {{ section.settings.torus_visible }},
      "mobileVisible": {{ section.settings.torus_mobile_visible | default: section.settings.torus_visible }}
    },
    "cone": { 
      "x": {{ section.settings.cone_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.cone_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.cone_mobile_x | default: section.settings.cone_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.cone_mobile_y | default: section.settings.cone_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.cone_rotation }}, 
      "visible": {{ section.settings.cone_visible }},
      "mobileVisible": {{ section.settings.cone_mobile_visible | default: section.settings.cone_visible }}
    },
    "spring1": { 
      "x": {{ section.settings.spring1_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.spring1_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.spring1_mobile_x | default: section.settings.spring1_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.spring1_mobile_y | default: section.settings.spring1_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.spring1_rotation }}, 
      "visible": {{ section.settings.spring1_visible }},
      "mobileVisible": {{ section.settings.spring1_mobile_visible | default: section.settings.spring1_visible }}
    },
    "spring2": { 
      "x": {{ section.settings.spring2_x | divided_by: 10.0 }}, 
      "y": {{ section.settings.spring2_y | divided_by: 10.0 }}, 
      "mobileX": {{ section.settings.spring2_mobile_x | default: section.settings.spring2_x | divided_by: 10.0 }}, 
      "mobileY": {{ section.settings.spring2_mobile_y | default: section.settings.spring2_y | divided_by: 10.0 }}, 
      "rotation": {{ section.settings.spring2_rotation }}, 
      "visible": {{ section.settings.spring2_visible }},
      "mobileVisible": {{ section.settings.spring2_mobile_visible | default: section.settings.spring2_visible }}
    }
  }
</script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/three.js/r128/three.min.js"></script>
<script>
(function() {
  const canvas = document.getElementById('hero-3d-canvas');
  if (!canvas) return;

  const settingsEl = document.getElementById('hero-3d-settings');
  const settings = settingsEl ? JSON.parse(settingsEl.textContent) : {};

  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(50, window.innerWidth / window.innerHeight, 0.1, 1000);
  camera.position.z = 15;

  const renderer = new THREE.WebGLRenderer({ canvas, alpha: true, antialias: true });
  renderer.setSize(window.innerWidth, window.innerHeight);
  renderer.setPixelRatio(Math.min(window.devicePixelRatio, 2));

  const ambientLight = new THREE.AmbientLight(0xffffff, 0.6);
  scene.add(ambientLight);

  const directionalLight = new THREE.DirectionalLight(0xffffff, 0.8);
  directionalLight.position.set(5, 10, 7);
  scene.add(directionalLight);

  const fillLight = new THREE.DirectionalLight(0xffffff, 0.3);
  fillLight.position.set(-5, -5, 5);
  scene.add(fillLight);

  const whiteMaterial = new THREE.MeshStandardMaterial({
    color: 0xffffff,
    roughness: 0.15,
    metalness: 0.05
  });

  const shapes = [];

  const s1 = settings.sphere1 || { x: 5.5, y: 3.5, mobileX: 3, mobileY: 3, rotation: 0, visible: true, mobileVisible: true };
  if (s1.visible || s1.mobileVisible) {
    const sphereGeo1 = new THREE.SphereGeometry(0.8, 32, 32);
    const sphere1 = new THREE.Mesh(sphereGeo1, whiteMaterial);
    sphere1.position.set(s1.x, s1.y, 0);
    sphere1.rotation.z = s1.rotation * Math.PI / 180;
    scene.add(sphere1);
    shapes.push({ mesh: sphere1, baseX: s1.x, baseY: s1.y, mobileX: s1.mobileX, mobileY: s1.mobileY, speed: 0.8, amplitude: 0.3, visible: s1.visible, mobileVisible: s1.mobileVisible });
  }

  const s2 = settings.sphere2 || { x: -5.5, y: -3, mobileX: -3, mobileY: -3, rotation: 0, visible: true, mobileVisible: true };
  if (s2.visible || s2.mobileVisible) {
    const sphereGeo2 = new THREE.SphereGeometry(0.6, 32, 32);
    const sphere2 = new THREE.Mesh(sphereGeo2, whiteMaterial);
    sphere2.position.set(s2.x, s2.y, 0);
    sphere2.rotation.z = s2.rotation * Math.PI / 180;
    scene.add(sphere2);
    shapes.push({ mesh: sphere2, baseX: s2.x, baseY: s2.y, mobileX: s2.mobileX, mobileY: s2.mobileY, speed: 1.0, amplitude: 0.25, visible: s2.visible, mobileVisible: s2.mobileVisible });
  }

  const t = settings.torus || { x: 6, y: 1.5, mobileX: 3, mobileY: 1, rotation: 60, visible: true, mobileVisible: true };
  if (t.visible || t.mobileVisible) {
    const torusGeo = new THREE.TorusGeometry(0.6, 0.2, 16, 48);
    const torus = new THREE.Mesh(torusGeo, whiteMaterial);
    torus.position.set(t.x, t.y, 0);
    torus.rotation.x = t.rotation * Math.PI / 180;
    scene.add(torus);
    shapes.push({ mesh: torus, baseX: t.x, baseY: t.y, mobileX: t.mobileX, mobileY: t.mobileY, speed: 0.7, amplitude: 0.2, rotate: true, visible: t.visible, mobileVisible: t.mobileVisible });
  }

  const c = settings.cone || { x: 4, y: -2.5, mobileX: 2.5, mobileY: -2, rotation: 17, visible: true, mobileVisible: true };
  if (c.visible || c.mobileVisible) {
    const coneGeo = new THREE.ConeGeometry(0.5, 1.2, 32);
    const cone = new THREE.Mesh(coneGeo, whiteMaterial);
    cone.position.set(c.x, c.y, 0);
    cone.rotation.z = c.rotation * Math.PI / 180;
    scene.add(cone);
    shapes.push({ mesh: cone, baseX: c.x, baseY: c.y, mobileX: c.mobileX, mobileY: c.mobileY, speed: 0.9, amplitude: 0.2, rotate: true, visible: c.visible, mobileVisible: c.mobileVisible });
  }

  function createSpring(x, y, rotation) {
    const springGroup = new THREE.Group();
    const curve = new THREE.CatmullRomCurve3([]);
    const coils = 5;
    const height = 2;
    const radius = 0.3;
    
    for (let i = 0; i <= coils * 20; i++) {
      const ti = i / (coils * 20);
      const angle = ti * coils * Math.PI * 2;
      const px = Math.cos(angle) * radius;
      const py = ti * height - height / 2;
      const pz = Math.sin(angle) * radius;
      curve.points.push(new THREE.Vector3(px, py, pz));
    }
    
    const tubeGeo = new THREE.TubeGeometry(curve, 100, 0.06, 8, false);
    const spring = new THREE.Mesh(tubeGeo, whiteMaterial);
    springGroup.add(spring);
    springGroup.position.set(x, y, 0);
    springGroup.rotation.z = rotation * Math.PI / 180;
    scene.add(springGroup);
    return springGroup;
  }

  const sp1 = settings.spring1 || { x: -5.5, y: 3, mobileX: -3, mobileY: 2.5, rotation: -11, visible: true, mobileVisible: true };
  if (sp1.visible || sp1.mobileVisible) {
    const spring1 = createSpring(sp1.x, sp1.y, sp1.rotation);
    shapes.push({ mesh: spring1, baseX: sp1.x, baseY: sp1.y, mobileX: sp1.mobileX, mobileY: sp1.mobileY, speed: 0.6, amplitude: 0.25, rotate: true, visible: sp1.visible, mobileVisible: sp1.mobileVisible });
  }

  const sp2 = settings.spring2 || { x: 6.5, y: -1, mobileX: 3, mobileY: -1, rotation: 11, visible: true, mobileVisible: true };
  if (sp2.visible || sp2.mobileVisible) {
    const spring2 = createSpring(sp2.x, sp2.y, sp2.rotation);
    shapes.push({ mesh: spring2, baseX: sp2.x, baseY: sp2.y, mobileX: sp2.mobileX, mobileY: sp2.mobileY, speed: 0.75, amplitude: 0.2, rotate: true, visible: sp2.visible, mobileVisible: sp2.mobileVisible });
  }

  let time = 0;

  function animate() {
    requestAnimationFrame(animate);
    time += 0.016;

    shapes.forEach((shape, i) => {
      shape.mesh.position.y = shape.baseY + Math.sin(time * shape.speed + i) * shape.amplitude;
      if (shape.rotate) {
        shape.mesh.rotation.y += 0.005;
      }
    });

    renderer.render(scene, camera);
  }

  function onResize() {
    camera.aspect = window.innerWidth / window.innerHeight;
    camera.updateProjectionMatrix();
    renderer.setSize(window.innerWidth, window.innerHeight);
    
    const isMobile = window.innerWidth < 768;
    const scale = isMobile ? 0.8 : 1;
    
    shapes.forEach((shape) => {
      shape.mesh.scale.setScalar(scale);
      
      if (isMobile) {
        shape.mesh.visible = shape.mobileVisible !== false;
        if (shape.mobileX !== undefined) {
          shape.mesh.position.x = shape.mobileX;
        }
        if (shape.mobileY !== undefined) {
          shape.mesh.position.y = shape.mobileY;
          shape.baseY = shape.mobileY;
        }
      } else {
        shape.mesh.visible = shape.visible !== false;
        if (shape.baseX !== undefined) {
          shape.mesh.position.x = shape.baseX;
        }
        shape.mesh.position.y = shape.baseY;
      }
    });
    
    if (isMobile) {
      camera.position.z = 12;
    } else {
      camera.position.z = 15;
    }
  }

  window.addEventListener('resize', onResize);
  onResize();
  animate();
})();
</script>

{% schema %}
{
  "name": "Hero Landing",
  "tag": "section",
  "class": "hero-landing-wrapper",
  "settings": [
    {
      "type": "header",
      "content": "Headlines"
    },
    {
      "type": "text",
      "id": "headline_line1",
      "label": "Headline line 1 (italic)",
      "default": "Mini"
    },
    {
      "type": "text",
      "id": "headline_line2",
      "label": "Headline line 2",
      "default": "DigiCam"
    },
    {
      "type": "range",
      "id": "mobile_headline_size",
      "label": "Mobile headline size",
      "min": 2,
      "max": 6,
      "step": 0.25,
      "default": 3,
      "unit": "rem"
    },
    {
      "type": "header",
      "content": "Product"
    },
    {
      "type": "product",
      "id": "featured_product",
      "label": "Featured product"
    },
    {
      "type": "image_picker",
      "id": "product_image",
      "label": "Hero product image",
      "info": "Leave empty to use default camera image. Recommended: transparent PNG."
    },
    {
      "type": "text",
      "id": "button_text",
      "label": "Button text",
      "default": "Add to cart"
    },
    {
      "type": "url",
      "id": "button_link",
      "label": "Button link",
      "info": "Leave empty to link to the featured product page"
    },
    {
      "type": "header",
      "content": "Button position"
    },
    {
      "type": "range",
      "id": "button_horizontal_position",
      "label": "Horizontal position",
      "info": "0 = left edge, 50 = center, 100 = right edge",
      "min": 10,
      "max": 90,
      "step": 1,
      "default": 28,
      "unit": "%"
    },
    {
      "type": "range",
      "id": "button_vertical_position",
      "label": "Vertical position",
      "info": "0 = top, 50 = middle, 100 = bottom",
      "min": 10,
      "max": 70,
      "step": 1,
      "default": 35,
      "unit": "%"
    },
    {
      "type": "header",
      "content": "Arrow - Desktop"
    },
    {
      "type": "range",
      "id": "arrow_length",
      "label": "Arrow length",
      "min": 30,
      "max": 120,
      "step": 5,
      "default": 50,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "arrow_width",
      "label": "Arrow width",
      "min": 20,
      "max": 80,
      "step": 5,
      "default": 40,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "arrow_offset",
      "label": "Arrow spacing from button",
      "min": 0,
      "max": 40,
      "step": 2,
      "default": 8,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "arrow_rotation",
      "label": "Arrow rotation",
      "min": -180,
      "max": 180,
      "step": 5,
      "default": 0,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "Arrow - Mobile"
    },
    {
      "type": "checkbox",
      "id": "arrow_mobile_visible",
      "label": "Show arrow on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "arrow_mobile_length",
      "label": "Mobile arrow length",
      "min": 20,
      "max": 80,
      "step": 5,
      "default": 40,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "arrow_mobile_width",
      "label": "Mobile arrow width",
      "min": 15,
      "max": 60,
      "step": 5,
      "default": 30,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "arrow_mobile_rotation",
      "label": "Mobile arrow rotation",
      "min": -180,
      "max": 180,
      "step": 5,
      "default": 0,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🔵 Sphere 1 - Desktop"
    },
    {
      "type": "checkbox",
      "id": "sphere1_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "sphere1_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": 55
    },
    {
      "type": "range",
      "id": "sphere1_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": 35
    },
    {
      "type": "range",
      "id": "sphere1_rotation",
      "label": "Rotation",
      "min": -180,
      "max": 180,
      "step": 5,
      "default": 0,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🔵 Sphere 1 - Mobile"
    },
    {
      "type": "checkbox",
      "id": "sphere1_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "sphere1_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": 30
    },
    {
      "type": "range",
      "id": "sphere1_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": 30
    },
    {
      "type": "header",
      "content": "🔵 Sphere 2 - Desktop"
    },
    {
      "type": "checkbox",
      "id": "sphere2_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "sphere2_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": -55
    },
    {
      "type": "range",
      "id": "sphere2_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": -30
    },
    {
      "type": "range",
      "id": "sphere2_rotation",
      "label": "Rotation",
      "min": -180,
      "max": 180,
      "step": 5,
      "default": 0,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🔵 Sphere 2 - Mobile"
    },
    {
      "type": "checkbox",
      "id": "sphere2_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "sphere2_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": -30
    },
    {
      "type": "range",
      "id": "sphere2_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": -30
    },
    {
      "type": "header",
      "content": "🍩 Torus - Desktop"
    },
    {
      "type": "checkbox",
      "id": "torus_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "torus_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": 60
    },
    {
      "type": "range",
      "id": "torus_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": 15
    },
    {
      "type": "range",
      "id": "torus_rotation",
      "label": "Tilt angle",
      "min": 0,
      "max": 180,
      "step": 5,
      "default": 60,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🍩 Torus - Mobile"
    },
    {
      "type": "checkbox",
      "id": "torus_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "torus_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": 30
    },
    {
      "type": "range",
      "id": "torus_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": 10
    },
    {
      "type": "header",
      "content": "🔺 Cone - Desktop"
    },
    {
      "type": "checkbox",
      "id": "cone_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "cone_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": 40
    },
    {
      "type": "range",
      "id": "cone_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": -25
    },
    {
      "type": "range",
      "id": "cone_rotation",
      "label": "Tilt angle",
      "min": -90,
      "max": 90,
      "step": 5,
      "default": 15,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🔺 Cone - Mobile"
    },
    {
      "type": "checkbox",
      "id": "cone_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "cone_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": 25
    },
    {
      "type": "range",
      "id": "cone_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": -20
    },
    {
      "type": "header",
      "content": "🌀 Spring 1 - Desktop"
    },
    {
      "type": "checkbox",
      "id": "spring1_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "spring1_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": -55
    },
    {
      "type": "range",
      "id": "spring1_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": 30
    },
    {
      "type": "range",
      "id": "spring1_rotation",
      "label": "Tilt angle",
      "min": -90,
      "max": 90,
      "step": 5,
      "default": -10,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🌀 Spring 1 - Mobile"
    },
    {
      "type": "checkbox",
      "id": "spring1_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "spring1_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": -30
    },
    {
      "type": "range",
      "id": "spring1_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": 25
    },
    {
      "type": "header",
      "content": "🌀 Spring 2 - Desktop"
    },
    {
      "type": "checkbox",
      "id": "spring2_visible",
      "label": "Show on desktop",
      "default": true
    },
    {
      "type": "range",
      "id": "spring2_x",
      "label": "Horizontal position",
      "min": -80,
      "max": 80,
      "step": 5,
      "default": 65
    },
    {
      "type": "range",
      "id": "spring2_y",
      "label": "Vertical position",
      "min": -50,
      "max": 50,
      "step": 5,
      "default": -10
    },
    {
      "type": "range",
      "id": "spring2_rotation",
      "label": "Tilt angle",
      "min": -90,
      "max": 90,
      "step": 5,
      "default": 10,
      "unit": "°"
    },
    {
      "type": "header",
      "content": "🌀 Spring 2 - Mobile"
    },
    {
      "type": "checkbox",
      "id": "spring2_mobile_visible",
      "label": "Show on mobile",
      "default": true
    },
    {
      "type": "range",
      "id": "spring2_mobile_x",
      "label": "Mobile horizontal",
      "min": -40,
      "max": 40,
      "step": 2,
      "default": 30
    },
    {
      "type": "range",
      "id": "spring2_mobile_y",
      "label": "Mobile vertical",
      "min": -50,
      "max": 50,
      "step": 2,
      "default": -10
    },
    {
      "type": "header",
      "content": "Circular element"
    },
    {
      "type": "text",
      "id": "circular_text",
      "label": "Circular text",
      "default": "point and shoot  point and shoot  "
    },
    {
      "type": "range",
      "id": "circular_text_size",
      "label": "Text size - Desktop",
      "min": 6,
      "max": 16,
      "step": 1,
      "default": 11,
      "unit": "px"
    },
    {
      "type": "range",
      "id": "circular_text_mobile_size",
      "label": "Text size - Mobile",
      "min": 4,
      "max": 12,
      "step": 1,
      "default": 8,
      "unit": "px"
    },
    {
      "type": "image_picker",
      "id": "lens_image",
      "label": "Lens image"
    },
    {
      "type": "header",
      "content": "Colors"
    },
    {
      "type": "color",
      "id": "background_color",
      "label": "Background",
      "default": "#FDF8F3"
    },
    {
      "type": "color",
      "id": "gradient_lavender",
      "label": "Gradient lavender",
      "default": "#E8E0F0"
    },
    {
      "type": "color",
      "id": "gradient_peach",
      "label": "Gradient peach",
      "default": "#FFDAB3"
    }
  ],
  "presets": [
    {
      "name": "Hero Landing"
    }
  ]
}
{% endschema %}
